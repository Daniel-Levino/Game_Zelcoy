package entities;

import java.awt.image.BufferedImage;

public class CheckPoint extends Entity{

	public CheckPoint(int x, int y, int w, int h, BufferedImage sprite) {
		super(x, y, w, h, sprite);
	}
}
